# 입을만해 — UI/UX 리뉴얼 작업 지침

## 프로젝트 개요

날씨 기반 의류 추천 서비스 "입을만해"의 전체 UI/UX 리뉴얼.
레퍼런스는 `DESIGN-figma.md`이며, 완전히 따라하지 않고 **방향성과 철학**만 차용한다.

---

## 디자인 원칙

### 레퍼런스에서 차용할 것

- **흑백 베이스 시스템**: 대부분의 UI 요소(헤더, 카드, 텍스트)는 흑(#000)/백(#fff) + 소프트 서피스(#f7f7f5)로 처리
- **파스텔 컬러 블록**: 계절 테마 또는 주요 섹션 구분에 파스텔 블록을 적극 활용. 섹션 전환의 주된 장치로 사용
- **Pill 버튼**: 모든 CTA는 `border-radius: 50px` 필 형태. 네모 버튼 금지
- **타이포그래피 위계**: 크기보다 **굵기(weight)**로 위계 표현. 회색 텍스트 최소화
- **섀도우 절제**: 카드 구분은 섀도우 대신 `border: 1px solid #e6e6e6` 또는 배경색 전환으로 처리

### 사이트 고유 정체성 (유지할 것)

- 계절(봄/여름/가을/겨울) 테마 전환 — 계절별 파스텔 팔레트와 연결
- 날씨 정보 표시 구조 (아이콘 + 기온 + 상세 정보)
- 아바타 피팅룸 섹션의 레이어 시스템

---

## 색상 시스템

### 기본 (계절 무관)

| 역할 | 값 |
|---|---|
| 잉크(기본 텍스트) | `#000000` |
| 캔버스(기본 배경) | `#ffffff` |
| 서피스 소프트 | `#f7f7f5` |
| 헤어라인 | `#e6e6e6` |

### 계절 파스텔 팔레트

| 계절 | 메인 컬러 블록 | 보조 |
|---|---|---|
| 봄 | `#efd4d4` (핑크) | `#f4ecd6` (크림) |
| 여름 | `#c8e6cd` (민트) | `#dceeb1` (라임) |
| 가을 | `#f3c9b6` (코랄) | `#f4ecd6` (크림) |
| 겨울 | `#c5b0f4` (라일락) | `#1f1d3d` (네이비, 역색) |

계절 버튼 active 상태는 해당 계절 메인 컬러로 배경 변경.

---

## 타이포그래피

폰트는 `Inter` (variable weight) 사용. figmaSans 대체재.

| 역할 | 크기 | 굵기 | 사용처 |
|---|---|---|---|
| 히어로 헤드라인 | 48–64px | 340 | 메인 섹션 타이틀 |
| 섹션 헤드라인 | 26px | 540 | 컬러 블록 내 제목 |
| 카드 타이틀 | 20px | 700 | 카드 상품명 |
| 본문 | 16px | 330 | 일반 설명 텍스트 |
| 캡션/레이블 | 12px | 400 | 태그, 분류 레이블 (대문자) |
| 버튼 | 16px | 480 | 모든 CTA 버튼 |

---

## 컴포넌트 규칙

### 버튼

- **Primary**: `background: #000`, `color: #fff`, `border-radius: 50px`, padding `10px 24px`
- **Secondary**: `background: #fff`, `color: #000`, `border: 1px solid #000`, `border-radius: 50px`
- **계절 탭**: 기본 → 서피스 소프트 배경 / 선택 → 해당 계절 파스텔 배경

### 카드

- `border-radius: 8px`, `border: 1px solid #e6e6e6`, `padding: 16px`
- 섀도우: 없음 (hover 시에만 `box-shadow: 0 4px 16px rgba(0,0,0,0.06)`)

### 컬러 블록 섹션

- `border-radius: 16px`, `padding: 48px`
- 블록과 블록 사이는 반드시 흰 캔버스 구간이 존재해야 함 (최소 64px gap)

### 아바타 스펙 레이블

- 현재 각기 다른 색상 → 단일 흑백 시스템으로 통일
- `background: #000`, `color: #fff`, `border-radius: 4px`

---

## 레이아웃 & 반응형

- 최대 컨텐츠 너비: `1280px`
- 기본 섹션 수직 간격: `80px`
- 컬러 블록 내부 여백: `48px`

| 브레이크포인트 | 변경 사항 |
|---|---|
| 960px (태블릿) | 2단 → 1단 재배치, 계절 탭 가로 스크롤 |
| 768px (모바일) | 컬러 블록 full-bleed (라운드 제거), 히어로 폰트 축소 |
| 560px (모바일 소) | 버튼 full-width, 카드 그리드 1열 |

---

## 하면 안 되는 것

- 회색 텍스트로 위계 표현 (weight로 대신할 것)
- 섀도우를 섹션 구분 장치로 사용
- 네모 버튼 사용
- 한 뷰포트 안에 컬러 블록 2개 이상 동시 노출
- 아바타 스펙 레이블에 각기 다른 랜덤 색상 사용

---

## 작업 방식

1. CSS 변수(`--color-*`, `--radius-*`, `--spacing-*`)로 토큰 정의 후 전체 적용
2. 섹션 단위로 작업, 한 섹션 완료 후 다음으로 이동
3. 계절 테마는 CSS 클래스 교체(`.spring`, `.summer` 등)로 처리 — JS 로직 최소 변경
4. `index.html` 구조 변경 최소화, 주로 CSS에서 해결
